{
  "name": "smiley-mobile",
  "version": "2.1.0",
  "private": true,
  "description": "Capacitor shell for Smiley companion UI (no Discord RPC on mobile)",
  "scripts": {
    "sync": "npx cap sync",
    "android": "npx cap open android",
    "ios": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0",
    "@capacitor/ios": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  },
  "capacitor": {
    "note": "Run npm install from mobile/ then cap add android|ios"
  }
}
