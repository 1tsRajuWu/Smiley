# Smiley platform note — v8 native (in progress)

Electron **v7.9.24** remains the current downloadable desktop build.

In parallel, **Smiley v8** (Tauri + Rust) is the new shell:

- Faster cold start, lower memory goal
- Same activities / PayPal tip / Discord Application ID model (not a bot)
- Animated wallpaper with tray pause
- Light gaming probe while full game-sync ports from `electron/game-sync.js`

Website: upgrade banner + v7.9.24 download rows.
Database: `scripts/migrate-v8-columns.sql` (richer install columns for skins / runtime / games).

Reference: `PLATFORM-UPGRADE.md`, `legacy/electron-v7/README.md`.
