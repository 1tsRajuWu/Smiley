# Smiley v8.0.12 — Valorant presence hotfix

## Valorant Discord rich presence

- **Score** — TDM `NumPoints`, spike round scores, FFA kills; chat `partyOwnerMatchScore*` fallback; no fake 0-0
- **Maps** — full HURM_* catalog (`HURM_Bowl` → Kasbah), Summit, **The Range** (`Rangev2`), etc.
- **Agent** — `CharacterID` from pregame/core-game including `PlayerIdentity`, object-mapped `Players`
- **Artwork** — Riot CDN Valorant V logo (`large_image`); agent or map icon (`small_image`)
- **Roster** — ally/enemy lines on Discord state; in-app LIVE MATCH board from local player lists
- **Endpoints** — chat session/presences, pregame, core-game, scoreboard probe, name-service (127.0.0.1 only)

## Settings

- Simplified **Gaming rich presence** section in Discord settings tab

## Install

GitHub Releases → `v8.0.12` (Windows installer, macOS DMG, Linux AppImage/deb).
