# Smiley v8.0.3

**Check for updates** in Settings → App and the tray menu. Compares your build against the latest `v8.*` GitHub release and links to the release page when a newer version is available.

Also removes third-party product name references from in-app copy — Valorant presence stays local Riot API only.
